; npm 10.9.x arborist 解析 vitest@4 optional peer(@vitest/browser-*) 时触发
; "Cannot read properties of null (reading 'edgesOut')" 已知 bug（见 docs/IMPLEMENTATION_PLAN.md P0 决策记录）。
; 采用 legacy-peer-deps 绕过 peer 自动解析；vitest 实际按精确版本锁定，不影响可复现性。
legacy-peer-deps=true
